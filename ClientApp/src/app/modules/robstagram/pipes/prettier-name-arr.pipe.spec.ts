import { PrettierNameArrPipe } from './prettier-name-arr.pipe';

describe('PrettierNameArrPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettierNameArrPipe();
    expect(pipe).toBeTruthy();
  });
});
