import { PrettierNameArrDirective } from './prettier-name-arr.directive';

describe('PrettierNameArrDirective', () => {
  it('should create an instance', () => {
    const directive = new PrettierNameArrDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
